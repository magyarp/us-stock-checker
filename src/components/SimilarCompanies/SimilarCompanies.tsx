import useStyles from "./SimilarCompanies.styles.ts";

function SimilarCompanies() {
  const { classes } = useStyles();

  return <div className={classes.root}>SimilarCompanies</div>;
}

export default SimilarCompanies;
